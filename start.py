import os

def fdx():
    strip = input('Enter your pin - code:     ')
    def finder(strip):
        now = os.getcwd()
        counter = 0
        while len(strip) != 1:
            for
                if os.path.exists('strip') is True:
                    counter += 1
        return counter

print(fdx())



